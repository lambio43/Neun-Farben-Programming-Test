using NF.Main.Core;
using UnityEngine;
using UnityEngine.Events;

public class BasePickup : MonoExt, IPickup
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public virtual void ActivatePickupEffect(BaseUnit baseUnit)
    {
        
    }
}
